import { NameShortenerPipe } from './name-shortener.pipe';

describe('NameShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new NameShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
